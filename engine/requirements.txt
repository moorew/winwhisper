# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
sse-starlette>=2.1.0
websockets>=12.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0

# Validation
pydantic>=2.7.1
pydantic-settings>=2.3.0

# Transcription
faster-whisper>=1.0.3

# Diarization
pyannote.audio>=3.3.1
torch>=2.3.0
torchaudio>=2.3.0

# Model management
huggingface-hub>=0.23.0

# Audio - Windows WASAPI loopback
# 0.2.12.8 is the current latest on PyPI; no 0.2.13 exists.
# Pinning to 0.2.13+ silently broke the Windows release wheel build.
pyaudiowpatch>=0.2.12.8; sys_platform == "win32"
soundfile>=0.12.1
numpy>=1.26.4

# YouTube
yt-dlp>=2024.5.1

# Watch folder
watchdog>=4.0.1

# Dictation / global hotkeys
keyboard>=0.13.5; sys_platform == "win32"
pynput>=1.7.6

# Export
python-docx>=1.1.2

# Utilities
python-dotenv>=1.0.1
