{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. Without a capability file at all, Tauri 2 denies every core plugin command — including event.listen, which is how the frontend receives tauri://drag-drop and the engine-ready signal.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
